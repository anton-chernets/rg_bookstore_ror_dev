- @orders.each do |o|

  %h3= o

  = render partial: 'shopping/cart/cart_items', locals: { items: o.order_items, edit: false }
  = render partial: 'Shopping/helpers/order', locals: { order: o, edit: false }

  %hr.top-buffer

.text-center
  = paginate @orders
  = page_entries_info @orders